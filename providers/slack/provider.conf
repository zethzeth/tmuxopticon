# Alarms — new messages from watched Slack channels. See slack-alarm-watch.sh.
# pull.sh is a thin adapter onto that script's `poll` subcommand (it reads its
# cache path from $SLACK_ALARM_CACHE rather than $1).
id=slack
title=Alarms
flag=SLACK_PULL_ENABLED
pull=pull.sh
order=30
timeout=45
